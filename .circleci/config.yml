version: 2.1
jobs:
  build:
    docker:
      - image: circleci/python:3.6.2-stretch-browsers
    steps:
      - checkout
      - python/load-cache
      - python/save-cache
      - run: pip install -r requirements/dev.txt